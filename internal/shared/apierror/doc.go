// Package apierror defines the project's single JSON error envelope and the
// HTTP status codes it maps to, so every feature returns errors in the same
// shape instead of inventing its own per feature.
package apierror
